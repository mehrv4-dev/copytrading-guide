# Task Future — دسترسی اختصاصی به ربات شخصی (بر اساس درخواست مشتری)

> این فایل **بخشی از راهنمای عمومی مشتری نیست** — فقط برای ثبت داخلی در همین ریپازیتوری نگه‌داری می‌شود.
> از سایت منتشرشده (`index.html`) به این فایل هیچ لینکی وجود ندارد.

وضعیت: **برنامه‌ریزی‌شده، هنوز ساخته نشده** (roadmap only).

## ایده

سرویس کپی‌تریدینگ فعلی (`@FractalMatrixFX` روی لایت‌فایننس) رایگان و خودکار است، ولی محدود به
مکانیزم Copy Trading خودِ بروکر است — فقط برای مشتری‌های همان بروکر کار می‌کند و مقیاس‌پذیری‌اش
به تنظیمات Copy type/Min investment بروکر وابسته است (نه به منطق خودِ ربات).

پیشنهاد: یک لایهٔ خدماتی جدید و **مبتنی بر درخواست** اضافه شود:

1. **محرک (Trigger):** مشتری خودش درخواست بدهد — چیزی که فعال یا تبلیغ نمی‌شود، فقط در دسترس است اگر خواسته شود.
2. **درخواست به ما ارسال شود** (کانال دقیق بعداً مشخص شود — تلگرام؟ فرم؟).
3. **در صورت تأیید،** مشتری با پرداخت **شارژ ماهانهٔ کم و منطقی** (عدد دقیق هنوز تعیین نشده) به یک
   **نمونهٔ اختصاصی و شخصی از ربات** دسترسی پیدا می‌کند.
4. مشتری خودش هر زمان خواست آن را **استارت/استاپ** می‌کند — کنترل کامل دست خودش است.

## چرا این با Copy Trading فرق دارد

Copy Trading فعلی نیاز به نصب یا اجرای هیچ کدی روی سیستم مشتری ندارد. این قابلیت جدید برعکس است:
مستلزم **ساخت و اجرای یک نمونهٔ واقعی از ربات** (منطق `mt5_v4_engine.py` و بقیهٔ موتور) مخصوص حساب
همان مشتری است — دقیقاً همان معماری‌ای که قبلاً برای worker 9001/9002 ساختیم، یا نسخه‌های
Matrix v1/v2/v3 که در نقشهٔ راه زیر طراحی شده بودند:

- `docs/matrix-bot-versions-roadmap.md` (در ریپازیتوری اصلی پروژه)
- حافظهٔ Claude: `matrix-bot-versioning-plan.md`

یعنی این تسک عملاً همان کاری‌ست که قبلاً برای بهینه‌سازی ربات بر اساس بازهٔ اکوییتی (v1/v2/v3) برنامه‌ریزی
شده بود، فقط این‌بار با یک مدل درآمدی مشخص (اشتراک ماهانه) و یک محرک مشخص (درخواست مشتری) بسته‌بندی شده.

## حساب تست پیش‌فرض (Matrix v1)

برای تست و تحلیل مداوم همین بازهٔ پایین (v1 → $۱۰ تا $۱۰۰)، یک حساب **$۱۰** به‌عنوان مرجع پیش‌فرض
تعیین خواهد شد — شمارهٔ دقیق حساب بعداً توسط کاربر داده می‌شود و اینجا تکمیل می‌گردد.

> ⚠️ یادآوری از تحلیل قبلی: حساب $۱۰ فقط با **اهرم ۱:۱۰۰۰** روی این بروکر قابل‌استفاده است — با
> اهرم پیش‌فرض ۱:۱۰۰ حتی یک اردر ۰.۰۱ لاتی هم باز نمی‌شود (کمبود مارجین). قبل از هر تستی این را
> روی همان حساب تنظیم کنید.

## باز (Open items)

- [ ] عدد دقیق شارژ ماهانه
- [ ] کانال دقیق دریافت درخواست مشتری
- [ ] شمارهٔ حساب $۱۰ مرجع (منتظر کاربر)
- [ ] طراحی فنی خودِ «نمونهٔ اختصاصی ربات» (احتمالاً بر پایهٔ Matrix v1، طبق نقشهٔ راه موجود)
